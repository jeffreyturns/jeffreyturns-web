import { CSSProperties } from 'vue';

export const fullHeight: CSSProperties = {
    height: '64px',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'start'
};

export const centeredVRow: CSSProperties = {
    height: 'calc((100% - 100px) - 0px)'
};
